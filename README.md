# Laura Portfolio Backend

Mismo motor que portfolio-backend, dedicated DB para Laura. NestJS + GraphQL + Mongo + JWT.

- Health: `GET /health`
- GraphQL: `POST /graphql`
- Bootstrap admin (1-shot): `mutation { bootstrapAdmin(username, password, secret) }`

## URL prod
https://backend.santillan.pro
